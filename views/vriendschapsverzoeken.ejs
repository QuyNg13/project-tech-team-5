<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Friend requests</title>

    <link rel="stylesheet" href="\style\style.css">
    <link href="https://fonts.cdnfonts.com/css/techno-race" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.10.7/dist/sweetalert2.all.min.js"></script>
</head>
<body>
    <h1>Vriendschapsverzoeken</h1>
    <p>requestid:<%= friendshipRequests %></p>
    <ul id="vrienden">
        <% if ((friendshipRequests !== undefined )&& (friendshipRequests.length > 0)) { %>
            <% friendshipRequests.forEach(request => { %> 
            <li>
                <a href="/profile/<%= request._id %>">
                    <img src="/style/images/uploads/<%= request._id %>/<%= request.profilePic %>" alt="Profielfoto">
                    <span><%= request.username %></span>
                </a>
                <form action="/accept-friend-request/<%= request.toString() %>" method="POST">
                    <button type="submit">Accepteren</button>
                </form>
                <p>requestid:<%= request._id %></p>
            </li>
            <% }) %>
        <% } else { %>
            <p>Er zijn geen vriendschapsverzoeken</p>
        <% } %>
    </ul>
</body>
</html>